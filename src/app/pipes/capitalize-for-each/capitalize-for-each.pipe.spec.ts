import { CapitalizeForEachPipe } from './capitalize-for-each.pipe';

describe('CapitalizeForEachPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeForEachPipe();
    expect(pipe).toBeTruthy();
  });
});
